<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Escape Room Didattica</title>
    <style>
        /* CSS: Stile e Atmosfera */
        body {
            font-family: 'Georgia', serif;
            background-color: #2c3e50;
            color: #ecf0f1;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            background-color: #34495e;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            max-width: 500px;
            text-align: center;
            border: 2px solid #f1c40f;
        }

        .hidden { display: none; }

        button {
            background-color: #f1c40f;
            border: none;
            padding: 10px 20px;
            color: #2c3e50;
            font-weight: bold;
            cursor: pointer;
            border-radius: 5px;
            font-size: 16px;
            transition: 0.3s;
        }

        button:hover { background-color: #d4ac0d; }

        input {
            padding: 10px;
            border-radius: 5px;
            border: none;
            margin: 10px 0;
            width: 80%;
        }

        .hint {
            font-size: 0.9em;
            color: #bdc3c7;
            font-style: italic;
            margin-top: 15px;
        }
    </style>
</head>
<body>

    <div id="page1" class="container">
        <h1>Il Custode del Tempo</h1>
        <p>Benvenuto, studioso. Sei rimasto bloccato nella Biblioteca Antica. Le porte si apriranno solo a chi dimostra di conoscere la storia e la cultura.</p>
        <p>Sei pronto a metterti alla prova?</p>
        <button onclick="showPage(2)">Inizia l'Avventura</button>
    </div>

    <div id="page2" class="container hidden">
        <h2>La Porta Sigillata</h2>
        <p>Sulla porta d'oro ci sono due incavi. Per sbloccarli devi rispondere correttamente:</p>
        
        <div style="text-align: left; margin: 20px 0;">
            <label>1. In che anno Cristoforo Colombo arrivò nelle Americhe?</label>
            <input type="number" id="ans1" placeholder="Es: 1000">
            
            <label>2. Chi scrisse la "Divina Commedia"? (Solo il nome)</label>
            <input type="text" id="ans2" placeholder="Es: Alessandro">
        </div>

        <button onclick="checkAnswers()">Tenta di Aprire la Porta</button>
        
        <p id="error-msg" style="color: #e74c3c; margin-top: 10px;"></p>

        <div class="hint">
            Indizio: Guarda tra i libri di storia del XV secolo e cerca il poeta fiorentino per eccellenza.
        </div>
    </div>

    <div id="page3" class="container hidden">
        <h1 style="color: #f1c40f;">Libero!</h1>
        <p>Incredibile! Hai dimostrato di possedere le chiavi della conoscenza.</p>
        <p>Le porte della biblioteca si sono spalancate grazie al tuo sapere.</p>
        <div style="font-size: 50px;">🔓📚</div>
        <br>
        <button onclick="location.reload()">Gioca di nuovo</button>
    </div>

    <script>
        // Funzione per navigare tra le "pagine"
        function showPage(pageNumber) {
            // Nascondi tutte le pagine
            document.getElementById('page1').classList.add('hidden');
            document.getElementById('page2').classList.add('hidden');
            document.getElementById('page3').classList.add('hidden');

            // Mostra quella desiderata
            document.getElementById('page' + pageNumber).classList.remove('hidden');
        }

        // Logica del gioco
        function checkAnswers() {
            const r1 = document.getElementById('ans1').value;
            const r2 = document.getElementById('ans2').value.trim().toLowerCase();
            const error = document.getElementById('error-msg');

            // Soluzioni: 1492 e dante
            if (r1 === "1492" && r2 === "dante") {
                showPage(3);
            } else {
                error.innerText = "I sigilli non si muovono... le risposte sono errate.";
                // Effetto scossa per errore
                const container = document.querySelector('#page2');
                container.style.transform = "translateX(10px)";
                setTimeout(() => container.style.transform = "translateX(0)", 100);
            }
        }
    </script>

</body>
</html>